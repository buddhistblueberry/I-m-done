<?xml version="1.0" encoding="utf-8"?>
<!--
  Freeflix launcher icon foreground (raster).

  Replaces the previous Netflix "N" vector with the new Freeflix brand
  mark (red play triangle + golden film-strip wave). Sized for the
  108x108 adaptive-icon viewport; the design's own black background
  blends with the solid-black adaptive background so legacy and
  adaptive icons render identically.
-->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/ic_launcher_foreground_bitmap" />
