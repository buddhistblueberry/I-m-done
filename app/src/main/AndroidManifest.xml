<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Foreground service for the headless Kodi-like stream engine
         (runs the LookMovieTomb addon flow in the background; no UI/WebView).
         POST_NOTIFICATIONS lets the low-priority service notification show
         on Android 13+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Declare touchscreen as optional so it installs on TV -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:name=".MarioCartApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Mario Cart"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarioCart"
        android:banner="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">

        <!-- Main activity — handles both phone and TV launchers -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.MarioCart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fullscreen player activity -->
        <activity
            android:name=".ui.player.PlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.MarioCart.Player" />

        <!-- In-app Verification WebView -->
        <activity
            android:name=".ui.player.VerificationActivity"
            android:exported="false"
            android:theme="@style/Theme.MarioCart" />

        <!-- Headless Kodi-like stream engine (foreground service).
             Runs the LookMovieTomb addon flow in the background; NO WebView.
             Keeps resolved streams warm so PlayerActivity can play instantly. -->
        <service
            android:name=".data.engine.KodiEngineService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- FileProvider for sharing the downloaded APK with the system installer -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
